server:
  port: 8081

spring:
  application:
    name: booking-service

    # Cấu hình kết nối Config Server
  config:
    import: "optional:configserver:http://localhost:8888"

  # CẤU HÌNH DATABASE
  datasource:
    url: jdbc:mysql://localhost:3306/booking_service_of_booking_system?createDatabaseIfNotExist=true
    username: root
    password: 18032003
    driver-class-name: com.mysql.cj.jdbc.Driver

  # CẤU HÌNH FLYWAY
#  flyway:
#    enabled: true
#    # Đảm bảo đường dẫn này khớp với thư mục bạn đã tạo ở bước trước (db/migration)
#    locations: classpath:db/migration
#    baseline-on-migrate: true